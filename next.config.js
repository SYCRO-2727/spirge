/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    cssModules: {
        folders: {
          styles: {
            global: true,
          },
        },
      },
    };